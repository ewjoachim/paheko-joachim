{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"type": "object",
	"properties": {
		"type": {
			"type": "string",
			"enum": ["reglement"]
		},
		"member_id": {
			"type": "integer",
			"description": "Membre qui a réglé. Un règlement solde la dette du membre, pas un chèque annulé en particulier : c'est le seul rattachement, et c'est lui qu'on lie à l'écriture (linked_users). Voir _member_debt.html."
		},
		"amount": {
			"type": "integer",
			"description": "Montant encaissé, en centimes. Un règlement partiel est permis : plusieurs documents s'additionnent alors face aux créances du membre."
		},
		"date": {
			"type": "string",
			"description": "Date d'encaissement (YYYY-MM-DD) : c'est aussi la date de l'écriture."
		},
		"method_id": {
			"type": ["null", "integer"],
			"description": "Moyen de paiement de la caisse choisi, pour l'affichage. Le règlement ne passe PAS par la caisse : on n'emprunte que son vocabulaire et son compte."
		},
		"account": {
			"type": ["null", "string"],
			"description": "Compte débité (banque, caisse, compte d'attente si c'est un chèque), figé à la saisie."
		},
		"notes": {
			"type": ["null", "string"]
		},
		"reglement_txn_id": {
			"type": ["null", "integer"],
			"description": "Identifiant de l'écriture de règlement, créée via :api. Un document par écriture : c'est ce qui permet à ce règlement d'apparaître seul dans « à comptabiliser »."
		}
	},
	"required": ["type", "member_id", "amount", "date"]
}
