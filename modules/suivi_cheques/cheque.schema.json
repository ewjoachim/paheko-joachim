{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"type": "object",
	"properties": {
		"type": {
			"type": "string",
			"enum": [
				"cheque"
			]
		},
		"payment_id": {
			"type": "integer",
			"description": "Paiement caisse (plugin_pos_tabs_payments.id)."
		},
		"planned_month": {
			"type": [
				"null",
				"string"
			],
			"description": "Surcharge du mois d'encaissement (YYYY-MM). NULL = déduit du moyen de paiement et de la date de réception."
		},
		"cancelled": {
			"type": "integer",
			"enum": [
				0,
				1
			],
			"description": "1 = chèque annulé/remplacé, retiré des remises."
		},
		"reason": {
			"type": [
				"null",
				"string"
			],
			"description": "Motif de l'annulation."
		},
		"notes": {
			"type": [
				"null",
				"string"
			]
		},
		"annulation_txn_id": {
			"type": [
				"null",
				"integer"
			],
			"description": "Identifiant de l'écriture d'annulation créée via :api. Lien primaire immuable module <-> compta."
		},
		"batch_id": {
			"type": [
				"null",
				"string"
			],
			"description": "Identifiant du lot de remise une fois le chèque parti à la banque. Verrouille le chèque."
		}
	},
	"required": [
		"type",
		"payment_id"
	]
}
