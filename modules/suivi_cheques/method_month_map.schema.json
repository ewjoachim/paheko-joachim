{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"type": "object",
	"properties": {
		"type": {
			"type": "string",
			"enum": ["method_month_map"]
		},
		"method_id": {
			"type": "integer",
			"description": "Moyen de paiement « chèque » de la caisse (plugin_pos_methods.id)."
		},
		"month": {
			"type": "integer",
			"minimum": 1,
			"maximum": 12,
			"description": "Mois d'encaissement par défaut (1-12). L'année est la prochaine occurrence de ce mois à partir de la date de réception du chèque (cf. _resolve_month.html)."
		}
	},
	"required": ["type", "method_id", "month"]
}
