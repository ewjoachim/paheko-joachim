{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"type": "object",
	"properties": {
		"type": {
			"type": "string",
			"enum": ["method_month_map"]
		},
		"method_id": {
			"type": "integer",
			"description": "Moyen de paiement « chèque » de la caisse (plugin_pos_methods.id)."
		},
		"month": {
			"type": "integer",
			"minimum": 1,
			"maximum": 12,
			"description": "Mois d'encaissement par défaut (1-12). L'année est fixée par l'exercice comptable courant."
		}
	},
	"required": ["type", "method_id", "month"]
}
